<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.aokihome.glewmetv">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlewMeTv">
        <activity
            android:name="io.aokihome.glewmetv.HookupActivity"
            android:label="@string/title_activity_hookup"
            android:theme="@style/Theme.GlewMeTv.NoActionBar"></activity>
        <activity
            android:name="io.aokihome.glewmetv.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GlewMeTv.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>